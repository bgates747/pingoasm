# Development log — 2026-07-29

## Expanded single-model baselines

The existing Cube, EarthIco, and EarthUV fixture system was extended with
profile-driven RGBA2222 HeavyTank, Lara, Crash, Jet, and Airliner fixtures.
No assembly application was written model-by-model. The shared generator
produces all model includes, textures, absolute poses, and binaries.

Two independent workloads now exist for all eight models:

1. stationary Y-axis rotation; and
2. the qualified deterministic viewport/frustum sweep.

The shortened visual-review variants use no warmups and two rotations. All
models passed hardware visual review except the first Lara export, which was
upside down.

Lara's authoritative source was identified as
`src/blender/LaraCroft/LaraCroft.blend`. A new Blender 4 exporter produced
`LaraCroft.obj/.mtl` beside it using standard `-Z` forward, `+Y` up semantics
without historical renderer-compensation inversions. The fresh hardware
fixture rendered upright. Obsolete plain/root Lara exports and legacy
generated artifacts were moved recoverably to desktop trash.

The canonical stationary hardware suite captured all 1,760 expected records:

| Model | Mean | Equivalent FPS |
|---|---:|---:|
| Cube | 204.738 ms | 4.88 |
| EarthIco | 97.747 ms | 10.23 |
| EarthUV | 146.373 ms | 6.83 |
| HeavyTank | 73.151 ms | 13.67 |
| Lara | 50.100 ms | 19.96 |
| Crash | 46.900 ms | 21.32 |
| Jet | 48.776 ms | 20.50 |
| Airliner | 92.488 ms | 10.81 |

The viewport-motion suite also completed. The automatic run contained all
eight models; an initial analysis mistake treated the renderer sequence as
global, although it is scoped to a Pingo control. Manually repeated Jet and
Airliner runs differed from the automatic results by only +0.23% and -0.31%,
respectively. This provides useful repeatability evidence and reinforces the
TODO for an explicit model/run identity in diagnostic records.

## First multi-object scene

A separate profile-driven `benchmarks/orbit-scene` workload now renders
EarthUV with Crash, Lara, Jet, and Airliner orbiting it. This preserves every
single-model fixture and result as an independent baseline.

The deterministic scene initially used:

1. Earth at `(0,0,-1000)` and the camera at `(0,0,3200)`;
2. a 2,500-unit X-Z orbit with four 90-degree phase offsets;
3. two revolutions in 10-degree steps plus the exact closing pose;
4. three Earth Y rotations per orbit;
5. distinct half-integer XYZ spins that close only after two orbits;
6. three series of four warmups plus 73 measured frames; and
7. five independent RGBA2222 textures and one RGBA2222 render target.

The generator validates orbital closure, unique IDs, authoritative asset
dimensions, and the eZ80 executable-plus-staging-texture memory bound. The
assembled executable is 172,737 bytes; with its largest 262,144-byte texture,
the maximum eZ80 staging extent is 434,881 of 524,288 bytes.

The isolated current-Pingo emulator successfully uploaded all five textures,
created and rendered all five objects with mutual depth, completed the full
animation, restored video mode, and exited normally. Human visual review found
the result correct on the first run. This is emulator qualification only;
physical hardware remains the performance and final correctness authority.

Hard reasoning was used for the multi-object scene design and implementation.

### Denser orbit promoted

The initial 10-degree orbit fixture was superseded after visual review by the
2.5-degree version. The denser 289-pose sweep exposes substantially more
combinations of partial, complete, and absent object visibility and is
therefore the canonical fixture for upcoming culling experiments. It uses one
series and no warmups: its present purpose is exploratory renderer work and
visual fun, not strict comparative data capture.

The complete slow hardware review showed stable object identities and correct
motion. The earlier apparent model substitution was not reproduced and is
attributed to temporal aliasing and observer fatigue. The old 10-degree
measurement files remain only as historical evidence; the separate generated
10-degree fixture is no longer an active test target.

## Camera-motion exploration

The multi-object scene was extended with two independent, profile-driven
camera fixtures. Neither replaces the qualified object-orbit fixture.

### Linear camera dolly

The first fixture isolated absolute camera translation. Earth and its four
companions were initially held at fixed world poses while the camera moved
linearly on its Z axis:

```text
Z=10000 -> Z=2500 -> Z=10000
```

It used 289 poses, including both exact endpoints, and introduced no camera
rotation. Emulator review was visually correct. The original two-revolution
object choreography was then reinstated on the same timeline. The camera
completed one inward-and-outward oscillation while all object positions and
orientations returned exactly to their starting state. Both emulator and
hardware review were visually correct.

### Polar elliptical camera orbit

A separate fixture then exercised camera translation and rotation together.
Earth is one focus of an ellipse in the world Y-Z plane. The camera:

1. starts at `(0,0,10000)`, 11,000 units from Earth;
2. sweeps at constant angle rather than simulated orbital velocity;
3. passes over the north pole;
4. reaches periapsis 2,500 units beyond Earth's far side at `(0,0,-3500)`;
5. passes over the south pole; and
6. returns exactly to its starting position and orientation.

The radius is generated from:

```text
a = (apoapsis + periapsis) / 2
e = (apoapsis - periapsis) / (apoapsis + periapsis)
p = a(1 - e²)
r = p / (1 - e cos(theta))
```

The angular sweep is deliberately uniform; this is geometric test motion, not
Keplerian orbital mechanics. The camera completes one ellipse while the
objects complete two orbits.

An unrotated camera faces local `-Z`. On this polar path, a continuous X
rotation from 0 through -360 degrees always points that axis toward Earth.
Camera Y rotation and local roll remain zero. This choice is both simple and
important: at the polar crossings, camera-forward is parallel to the usual
world-up vector. A conventional `lookAt(position, target, world_up)` is
singular there. Prescribing the continuous X angle chooses the roll frame by
continuity while deliberately traversing the +/-90-degree pitch conditions
associated with Euler gimbal lock.

The TV-compatible assembly helper did not previously expose the absolute
three-axis camera-rotation command. `scrabs` was added using Pingo subcommand
21; an older helper elsewhere incorrectly labels a three-axis payload as
subcommand 18, which accepts only one X angle and must not be copied.

Eleven generator and mathematical regression tests now cover the canonical
orbit, camera dolly, elliptical endpoints, polar crossing, camera angles,
object motion, exact closure, validation, and eZ80 staging-memory bound. The
polar fixture assembled successfully and passed visual review in both the
qualified emulator and physical hardware. This establishes camera pose
translation and rotation—including smooth passage through both poles—as
working for this renderer state.

## Selective modern-upstream optimization sweep

The Author granted a free hand under Ultra reasoning to extract useful work
from modern `fededevi/pingo` before pursuing less attributable renderer
experiments. The principal risk was confirmed: modern upstream is not a
drop-in library update. Its `Entity`/callback object model, transform
ownership, camera inversion, mesh UV ownership, and backend interfaces have
diverged substantially from the TurboVega-era code retained by Agon. The
object-model rewrite was therefore treated as context, not as a merge target.

### Silent emulator benchmark control

The owned Fab fork now has `scripts/benchmark-pingo.py`, a strict headless
runner that uses dummy SDL video and audio drivers. It opens no window and
plays no boot beep. Each repeat starts a fresh emulator process with an
explicit VDP module, MOS image, simulated SD card, software renderer,
zero-initialized RAM, and unlimited eZ80 execution.

The harness requires:

1. one active `LOAD` followed by one active `RUN` in `autoexec.txt`;
2. an existing selected executable while preserving the established
   project-local SD symlink convention;
3. contiguous `PINGO_RENDER` sequence numbers and declared bitmap IDs/counts;
4. stable SHA-256 identities for all benchmark inputs and the harness itself;
5. clean debugger-assisted shutdown; and
6. staged publication, with the JSON report written last so a failed forced
   rerun cannot pair stale metadata with partial or replaced logs.

The installed emulator helper suite now has 28 passing tests. A complete
289-frame silent integration run also passed. Emulator timing is suitable for
same-host candidate screening only; ESP32 hardware remains the performance
and final visual authority.

### Accepted upstream-derived changes

Three deliberately small changes survived isolation:

1. `vec3Normalize` adopts the guarded scalar method from upstream
   `fb67d951c4f05ca5fa04c45dcb3cb3b02f861163`: zero and exact-unit fast
   paths, one square root, one reciprocal, and three multiplies instead of
   three divisions.
2. `mat4Inverse` adopts the same commit's exact translation-only fast path,
   which also covers identity. Upstream's separate identity predicate was
   removed as redundant, and its scale-only path was rejected because it
   takes unguarded reciprocals of scale components.
3. View and projection are composed once per object and applied with one
   matrix-vector product per triangle vertex. This is a locally derived
   VP-only adaptation inspired by upstream transform-composition lineage,
   notably `a0ed0cb`; it is not a literal port. It preserves the Agon port's
   model-space lighting, camera-pose contract, and older object model.

The composed call relies on the established, counterintuitive contract that
`mat4MultiplyM(m1, m2)` returns `m2 × m1`; therefore
`mat4MultiplyM(&view, &projection)` correctly produces
`projection × view`.

A new native math test covers zero/unit/general normalization, identity,
translation-only and rotated general inverses, and a nontrivial
sequential-versus-composed view/projection transform. Ordinary and diagnostic
native suites pass. Both embedded PlatformIO builds also pass: ordinary
firmware uses 1,065,125 bytes of flash, and the diagnostic variant uses
1,067,385 bytes.

### Rejected upstream candidates

Measured isolation prevented several superficially attractive imports:

1. changing the affine matrix-vector helper to consume arbitrary input `w`
   slowed the ellipse fixture by 15.72%; current pre-projection inputs
   intentionally have `w=1`;
2. broad upstream raster cleanup slowed it by 3.47%, and a narrower cleanup
   slowed it by 2.30%;
3. upstream identity and translation special cases in general matrix
   multiplication added comparison overhead and no useful whole-frame gain;
4. moving VP composition from once per object to once per renderer invocation
   showed no repeatable benefit and added state; and
5. the `Entity` refactor, renderer-side camera inversion, scale-only inverse,
   singular-inverse behavior, hard-coded far distance, and composition inside
   the triangle loop were rejected on semantic or safety grounds.

The float-literal experiment remains open for eventual ESP32 measurement:
native/emulator results do not settle the cost of the embedded
double-precision helper calls seen in disassembly.

### Emulator timing result

The five-object polar ellipse fixture was used because it exercises a rotated
and translated camera, five independently transformed objects, and extensive
offscreen motion.

In a clean five-repeat A/B/A bracket, the pre-upstream-sweep frustum state
averaged 785.110 microseconds over 2,890 frames. The accepted upstream tranche
before removal of the redundant identity predicate averaged 700.246
microseconds over 1,445 frames: a 10.81% same-host reduction. The exact final
source, after that semantics-preserving cleanup, averaged 713.496 microseconds
over another 1,445 frames, a 9.12% reduction against the pooled controls.
The spread between the two optimized runs is treated as host variance, not as
evidence that a redundant comparison helps or hurts ESP32 performance.

Attribution supports a real transform saving. On the diagnostic ellipse run,
mean per-frame transform time fell from 476.369 to 439.682 microseconds
(-7.70%), while raster time was essentially unchanged (379.018 versus
379.512 microseconds).

### Render-target equivalence

A temporary, uncommitted Cube fixture requested one debug dump of target
bitmap 1257 after its final measured render. The probe decoded exactly 76,800
bytes (`320 × 240 × 1` RGBA2222). The pre-sweep, pre-VP, post-VP, and exact
final-source modules all produced:

```text
SHA-256 aa3033b451bd8fe6168a998073dd445738ec9ce1bd91b4a6dd515244b13fb859
```

This is exact final Pingo render-target equivalence, not a claim about every
intermediate frame, the z-buffer, or Fab's later bitmap display/composition.
The dump occurs after the timed record and does not contaminate `render_us`.
Human review of the exact current module passed with the five-object polar
ellipse fixture. Embedded hardware qualification is still required before
these firmware changes are accepted as hardware work.

## Short hardware fixture paths

The hardware SD card no longer needs to expose project-source hierarchy for
generated benchmark runtimes. The convention is now:

```text
/pingo/<fixture>/benchmark.bin
/pingo/<fixture>/<runtime textures>
```

There is no redundant `benchmarks/<suite>/fixtures` or `tgt` layer on the
card. The render-spin suite deployer, orbit-scene deployer, their tests, the
local `~/copy_to_sd.sh` helper, and canonical deployment documentation now
agree on this layout. Each deployer replaces only the named fixture children
under the shared `/pingo` root; deploying one suite cannot erase the other.

The mounted card received the eight qualified stationary single-model
fixtures plus all three multi-object orbit fixtures. Recursive comparisons
against every corresponding generated `tgt` directory found no differences.
The old long-path copies were preserved for now. Hardware boot selects only:

```text
cd /pingo/earth-party-camera-ellipse-rgba2222
load benchmark.bin
run
```

Eight deployment tests now cover the emitted paths, direct file layout,
preservation of unrelated fixtures, rejection of symlinked deployment roots,
and cross-suite name collisions. The local Bash helper also rejects empty,
dot, parent, and slash-containing fixture names. The card contents and
`autoexec.txt` are ready for a physical boot test; that test has not yet been
claimed here.

## Near-plane fixtures and overnight Hecker plan

Two dedicated RGBA2222 near-plane fixtures now use the profile-driven
render-spin generator. Cube and EarthUV each rotate one revolution in 45
8-degree steps while the camera follows an absolute linear path from
`(0,0,3200)` to the object center `(0,0,0)` and back. The odd frame count
guarantees an exact center pose, so the path includes ordinary view, surface
approach, near-plane intersection, and camera-inside-mesh conditions.

Generator validation covers the start, turnaround, and return endpoints.
Both fixtures assemble and complete all 45 expected diagnostic records in
fresh silent emulator runs. The card copies match their generated `tgt`
directories, and its existing ellipse `autoexec.txt` selection was deliberately
left unchanged.

The proposed unattended Hecker workflow, automatic gates, card-resident
fixture order, evidence requirements, rollback policy, and known limitations
are recorded in `docs/hecker-overnight-test-plan.md`. This is a plan, not an
implementation or an acceptance claim.

## Qualified unattended regression harness

The overnight chain was shortened after its first physical run proved too
heavily weighted toward repeated simple spins. Each of the eight stationary
single-model fixtures now performs one 36-frame revolution with no warmups.
Cube and EarthUV near-plane paths were expanded, and equivalent Jet and
Airliner paths were added. Each torture fixture renders 73 poses: two
10-degree revolutions including the closing 720-degree pose, phased against
one camera cycle from `Z=3200` through the mesh center at `Z=0` and back.

The first revised hardware chain failed after 616 records, immediately after
Jet near-plane and while creating the next bitmap. Its ESP32 backtrace resolves
through `createEmptyBitmap()` and `bufferWrite()`, confirming cumulative
resource allocation rather than a rasterizer crash. The four torture profiles
had unnecessarily distinct control and bitmap IDs. Rebuilding them with the
same proven IDs used by the other single-model fixtures allows each application
to replace prior resources.

The corrected physical chain passed:

1. 1,447 total render records;
2. bitmap 1257 delivered contiguous sequence `0..579`;
3. bitmap 1410 delivered contiguous sequence `0..866`; and
4. no panic, reboot, gap, duplicate, or sequence reset.

The qualified raw log is
`benchmarks/render-spin/results/chained-suite-shared-resource-ids-baseline-hardware-2026-07-29.log`,
SHA-256
`b23a5ff075650df656c6f912a9cd093c0d7f3e680994ac2d965ea7aa3a4845b2`.
The allocation-failure log remains separately named as negative harness
evidence and must not be used as a renderer baseline.

## Working pre-Hecker firmware

The current upstream-derived math tranche passed the same complete physical
chain. Relative to the preceding hardware firmware, every fixture improved:

| Fixture | Previous ms | Current ms | Render-time change |
|---|---:|---:|---:|
| Cube | 204.735 | 203.379 | -0.66% |
| HeavyTank | 73.146 | 72.841 | -0.42% |
| EarthUV | 146.425 | 144.026 | -1.64% |
| Cube near-plane | 264.991 | 263.416 | -0.59% |
| EarthUV near-plane | 228.933 | 225.864 | -1.34% |
| Jet near-plane | 107.240 | 105.347 | -1.77% |
| Airliner near-plane | 200.726 | 192.619 | -4.04% |
| EarthIco | 96.389 | 95.602 | -0.82% |
| Lara | 50.025 | 48.975 | -2.10% |
| Crash | 46.824 | 45.543 | -2.73% |
| Jet | 47.148 | 45.800 | -2.86% |
| Airliner | 91.130 | 84.060 | -7.76% |
| Earth party ellipse | 187.629 | 174.821 | -6.83% |
| Earth party | 183.076 | 170.097 | -7.09% |
| Earth party dolly | 148.249 | 135.782 | -8.41% |

The latest clean boot in
`chained-suite-upstream-math-candidate-hardware-2026-07-29.log` passed both
complete streams and contained no panic or reboot. Firmware commit `cb91c12`
is tagged `working-pre-hecker`. Its ordinary and diagnostic artifacts and
manifest are preserved outside Git under
`~/Agon/mystuff/pingo-firmware-archive/05-working-pre-hecker`.

The owned Fab harness now also understands chained suites as independent
expected bitmap streams. Against the exact current userspace VDP it accepted
all 1,447 records, reverified 42 runtime files across all 15 fixture
directories, and completed silently in one fresh process. Those emulator
harness changes remain uncommitted pending the project's explicit
human-validation rule.

## Exact-output rasterizer experiment

Work continued on `agon-vdp:experiment/hecker-rasterizer` from the exact
`working-pre-hecker` point. An emulator-only gate now hashes both the final
color target and complete 32-bit z-buffer after every render. Two independent
baseline processes reproduced all 1,447 states exactly; the compact oracle's
SHA-256 is
`abd2d61bb08d333923f7f79d17335267ba11e9754f0ece64feb567e252fdc11d`.

The retained work is deliberately evolutionary and consists of separate,
reversible commits. It hoists constant lighting work, removes redundant matrix
copies, caches backend state, fuses and inlines depth testing/writing, replaces
libc texture clamps, inlines the native texture/shading path, shades RGBA2222
through a four-level per-triangle lookup, and writes the default backend
directly.

A clean-room integer row-span primitive then replaced bounding-box scanning
outside each triangle. Its native test exhaustively compares both windings,
inclusive shared edges, clipped viewports, randomized affine rows, and
integer-limit cases with the old per-pixel predicate. Integration first kept
that predicate as a redundant guard; after all 1,447 color and depth states
matched, the guard was removed in commit `b87c95e`. Historical Hecker code was
used only as conceptual provenance: its projection, depth, UV, clipping, fill,
and backend behavior are not compatible, and no historical implementation was
copied.

Two attractive arithmetic shortcuts were rejected and fully reverted. A
single-precision area reciprocal changed one near-plane frame. Sharing one
perspective reciprocal between U and V changed color in 9 of 1,447 frames,
although every z-buffer remained identical. These are precisely the subtle
rounding/texel-boundary changes the new gate is intended to catch.

Three fresh ordinary emulator processes put the current candidate at a pooled
370.27 microseconds per render versus 586.38 for the pre-Hecker screening run,
a 36.86% reduction. This is evidence that the candidate deserves physical
testing, not a hardware performance claim. Native ordinary/diagnostic tests,
ordinary/diagnostic ESP32 builds, and the VDU scope check pass; embedded RAM
remains 42,520 bytes.

The execution environment's privileged upload approval timed out twice before
PlatformIO was started. No candidate was flashed, and the Agon remains on the
known-good pre-Hecker firmware. The candidate therefore remains explicitly
awaiting hardware timing and human visual qualification. The detailed ledger,
per-fixture emulator table, firmware identity, rejected experiments, and next
gate are in
`~/Agon/mystuff/agon-vdp/docs/pingo-rasterizer-experiment-2026-07-29.md`.
